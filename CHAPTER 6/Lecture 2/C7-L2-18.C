#include<stdio.h>
#include<conio.h>

main()
{
	int i,n,sum=0;
	clrscr();
	printf("enter positive no:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(sum+=i)
		{
			printf(" sum is :%d\n",sum);
		}

	}
	getch();
}